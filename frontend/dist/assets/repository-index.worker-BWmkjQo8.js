(function(){let e={has_updates:0,unreachable:1,no_remote:2,synced:3};function t(t,n){let r=n.query.trim().toLocaleLowerCase(`zh-CN`),i=t.filter(e=>n.filter===`dirty`&&!e.dirty||n.filter!==`all`&&n.filter!==`dirty`&&e.freshness!==n.filter?!1:r.length===0||`${e.name}\n${e.path}\n${e.branch??``}`.toLocaleLowerCase(`zh-CN`).includes(r));return i.sort((t,r)=>n.sort===`behind`?r.behindCount-t.behindCount||t.name.localeCompare(r.name,`zh-CN`):n.sort===`status`&&e[t.freshness]-e[r.freshness]||t.name.localeCompare(r.name,`zh-CN`)),i.map(e=>e.repoId)}let n=[];self.onmessage=e=>{let r=e.data;if(r.type===`replace`){n=r.repositories;return}self.postMessage({type:`result`,requestId:r.requestId,repoIds:t(n,r)})}})();